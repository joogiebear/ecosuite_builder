import {
  cleanObject,
  csvToArray,
} from '../../lib/schema.js';

const plugin = {
    id: 'EcoBits',
    name: 'EcoBits',
    group: 'Economy and Utility',
    accent: '#2b7a8f',
    requires: ['eco'],
    uses: [],
    description: 'Define custom currencies with their own symbols, formatting, commands, and balance rules.',
    reference: [
      'EcoBits is primarily a currency-definition plugin, not an effect holder.',
      'Once a currency is defined here, other Auxilor plugins can reference it through the shared price system.',
    ],
    templates: [
      {
        id: 'currency-config',
        name: 'Currency',
        description: 'Generate a starting EcoBits config with one currency entry.',
        getOutputPath: () => 'plugins/EcoBits/config.yml',
        initialValues: {
          serverId: 'main',
          leaderboardEnabled: true,
          leaderboardCacheLifetime: 60,
          shortcuts: ',k,M,B,T,P,E',
          currencyId: 'crystals',
          currencyName: 'Crystals',
          symbol: '✦',
          defaultBalance: 0,
          maxBalance: -1,
          payable: false,
          decimal: true,
          maxDecimals: 2,
          vault: false,
          local: false,
          balanceShorthand: false,
          commands: 'crystals,ecocrystals',
          format: '&b%symbol%&a%amount% &b%currency%',
          formatShort: '&b%symbol% %amount%',
          decimalFormat: '#,##0.00',
          decimalFormatShort: '#,##0.00',
          additionalCurrencies: [],
        },
        sections: [
          {
            title: 'Server and leaderboard',
            fields: [
              { key: 'serverId', label: 'Server ID', type: 'text', width: 'half' },
              { key: 'leaderboardEnabled', label: 'Leaderboard enabled', type: 'switch', width: 'half' },
              { key: 'leaderboardCacheLifetime', label: 'Leaderboard cache (seconds)', type: 'number', width: 'half' },
              { key: 'shortcuts', label: 'Shortcuts', type: 'text', width: 'full', help: 'Comma-separated, empty entries allowed. Example: ,k,M,B,T,P,E' },
            ],
          },
          {
            title: 'Primary currency',
            fields: [
              { key: 'currencyId', label: 'Currency ID', type: 'text', width: 'half' },
              { key: 'currencyName', label: 'Display name', type: 'text', width: 'half' },
              { key: 'symbol', label: 'Symbol', type: 'text', width: 'half' },
              { key: 'commands', label: 'Commands', type: 'text', width: 'half' },
              { key: 'defaultBalance', label: 'Default balance', type: 'number', width: 'half' },
              { key: 'maxBalance', label: 'Max balance', type: 'number', width: 'half' },
              { key: 'maxDecimals', label: 'Max decimals', type: 'number', width: 'half' },
              { key: 'payable', label: 'Payable', type: 'switch', width: 'half' },
              { key: 'decimal', label: 'Allow decimals', type: 'switch', width: 'half' },
              { key: 'vault', label: 'Register with Vault', type: 'switch', width: 'half' },
              { key: 'local', label: 'Local currency only', type: 'switch', width: 'half' },
              { key: 'balanceShorthand', label: 'Command acts as balance command', type: 'switch', width: 'half' },
              { key: 'format', label: 'Format', type: 'text', width: 'half' },
              { key: 'formatShort', label: 'Short format', type: 'text', width: 'half' },
              { key: 'decimalFormat', label: 'Decimal format', type: 'text', width: 'half' },
              { key: 'decimalFormatShort', label: 'Short decimal format', type: 'text', width: 'half' },
            ],
          },
          {
            title: 'Additional currencies',
            fields: [
              {
                key: 'additionalCurrencies',
                label: 'Extra currencies',
                type: 'collection',
                width: 'full',
                addLabel: 'Add currency',
                fields: [
                  { key: 'id', label: 'Currency ID', type: 'text', width: 'half' },
                  { key: 'name', label: 'Display name', type: 'text', width: 'half' },
                  { key: 'symbol', label: 'Symbol', type: 'text', width: 'half' },
                  { key: 'commands', label: 'Commands', type: 'text', width: 'half' },
                  { key: 'default', label: 'Default balance', type: 'number', width: 'half' },
                  { key: 'max', label: 'Max balance', type: 'number', width: 'half' },
                  { key: 'maxDecimals', label: 'Max decimals', type: 'number', width: 'half' },
                  { key: 'payable', label: 'Payable', type: 'switch', width: 'half' },
                  { key: 'decimal', label: 'Allow decimals', type: 'switch', width: 'half' },
                  { key: 'vault', label: 'Register with Vault', type: 'switch', width: 'half' },
                  { key: 'local', label: 'Local currency only', type: 'switch', width: 'half' },
                  { key: 'balanceShorthand', label: 'Command acts as balance', type: 'switch', width: 'half' },
                  { key: 'format', label: 'Format', type: 'text', width: 'full' },
                  { key: 'formatShort', label: 'Short format', type: 'text', width: 'full' },
                  { key: 'decimalFormat', label: 'Decimal format', type: 'text', width: 'half' },
                  { key: 'decimalFormatShort', label: 'Short decimal format', type: 'text', width: 'half' },
                ],
              },
            ],
          },
        ],
        toConfig: (values) => {
          const primary = {
            id: values.currencyId,
            name: values.currencyName,
            symbol: values.symbol,
            default: Number(values.defaultBalance),
            max: Number(values.maxBalance),
            payable: values.payable,
            decimal: values.decimal,
            'max-decimals': Number(values.maxDecimals),
            vault: values.vault,
            local: values.local,
            'balance-shorthand': values.balanceShorthand,
            format: values.format,
            'format-short': values.formatShort,
            'decimal-format': values.decimalFormat,
            'decimal-format-short': values.decimalFormatShort,
            commands: csvToArray(values.commands),
          };

          const extras = (values.additionalCurrencies ?? []).map((entry) =>
            cleanObject({
              id: entry.id,
              name: entry.name,
              symbol: entry.symbol,
              default: entry.default === '' || entry.default === undefined ? undefined : Number(entry.default),
              max: entry.max === '' || entry.max === undefined ? undefined : Number(entry.max),
              payable: entry.payable,
              decimal: entry.decimal,
              'max-decimals': entry.maxDecimals === '' || entry.maxDecimals === undefined ? undefined : Number(entry.maxDecimals),
              vault: entry.vault,
              local: entry.local,
              'balance-shorthand': entry.balanceShorthand,
              format: entry.format,
              'format-short': entry.formatShort,
              'decimal-format': entry.decimalFormat,
              'decimal-format-short': entry.decimalFormatShort,
              commands: csvToArray(entry.commands),
            }),
          );

          return {
            'server-id': values.serverId,
            leaderboard: {
              enabled: values.leaderboardEnabled,
              'cache-lifetime': Number(values.leaderboardCacheLifetime),
            },
            shortcuts: String(values.shortcuts ?? '').split(',').map((item) => item.trim()),
            currencies: [primary, ...extras.filter(Boolean)],
          };
        },
      },
    ],
  };

export default plugin;
